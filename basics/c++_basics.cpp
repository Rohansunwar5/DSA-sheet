#include <bits/stdc++.h> // includes all the libraries
// #include <math.h>
// #include <string>
using namespace std;

int main()
{

  // printing variable
  // cout << "Hey string" << endl
  //      << "Hey Rohan" << endl;
  // cout << "Hey string" << endl;

  // Taking Input:
  // int x, y;
  // cin >> x >> y;
  // cout << "value of x: " << x << " and value of y: " << y;

  // Data Type
  // int x = 10;
  // long y = 23;
  // cout << x << endl;

  // // float, double => they can store both double and integers and floating numbers
  // float f = 12.232323232323;
  // cout << f;

  // string and getline
  // the string only stores the data until the space is press after that if we need to get the input for the string we either have to declare a seperate variable for it

  // string s, d;
  // cin >> s >> d;
  // cout << s << " " << d;
  // in case if we want to take everything together within a string we can use the getline function
  // getline
  // string str;
  // getline(cin, str);
  // cout << str;

  // char
  // we can aslo use string function for storing a single character but it ends up taking up more space so char is more viable
  // char ch = 'g';
  // cout << ch;

  return 0;
}